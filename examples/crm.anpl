module crm

type Customer {
  id: uuid
  name: text
  age?: int
}

fn createCustomer(name: text) -> Customer {
  return Customer {
    id: uuid()
    name: name
  }
}

fn main() -> int {
  let customer = createCustomer("Ada")
  return len(customer.name)
}
